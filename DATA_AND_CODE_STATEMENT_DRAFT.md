# Data and code availability statement

Use the current statement below. It deliberately distinguishes executable code
and exact benchmark bytes from the still-missing historical row-level ledgers.

> The executable reference implementation, frozen configuration, exact
> downloaded UCR/UEA train/test files, source URLs, SHA-256/CRC receipts, and
> scripts used to validate the method and build the manuscript are available
> without login at https://github.com/HanZhang457/ASTRAL-Reproducibility.
> Historical row-level result ledgers underlying the numerical summary tables
> are not included in the current rebuild and those tables should not be treated
> as independently reproduced until the ledgers are restored or rerun.
